# 事件指令规则子代理任务契约

本契约用于把事件指令参数里的玩家可见文本整理成导入规则。执行者只按本文件、CLI 输出、工作区文件和任务说明工作，不读取项目源码、数据库或程序内部对象。

本任务是工作子代理任务。执行者除最终规则草稿外，还必须写工作报告和必要的审计材料；规则导入前会由外部规则审查代理复核。

## 输入

- 只读文件：`<工作区>/event-commands.json`。
- 任务参数：`<项目目录>`、`<工作区>`、`<游戏标题>`。
- 可参考信息：主代理可说明 `export-event-commands-json` 已成功生成 `<工作区>/event-commands.json`，并读取 stdout JSON 摘要。

## 逻辑

- 按事件指令编码分组检查 `parameters` 中的字符串叶子、字符串数组和嵌套对象。
- 工作区可能包含默认导出的插件命令编码，也可能包含 CLI 显式指定的任意事件指令编码；编码只表示导出起点，仍必须以当前 `event-commands.json` 顶层编码为准。
- 只选择玩家会在对话、菜单、选择、提示、任务、状态、说明或插件命令显示内容中看到的自然语言文本。
- 排除资源路径、文件名、脚本、公式、数字、颜色、坐标、布尔值、枚举值、开关名、内部标识、调试字段和纯配置键。
- 需要把同一编码下不同形态区分开时，用 `match` 精确限定参数索引和值；`match` 的键必须是参数索引字符串。
- 没有过滤条件时，`match` 写 `{}`。
- 插件命令或其他事件指令参数可能是嵌套对象、字符串数组、顶层字符串或单个命令字符串。必须以当前导出的 `event-commands.json` 为准；不要把某个编码、参数位置、插件名或字符串形态当成所有游戏通用答案。
- 字符串像 JSON 时，可以解析后继续判断里面的字符串叶子；最终 JSONPath 仍必须指向 `event-commands.json` 中真实存在的字符串叶子。
- 事件指令里的插件命令字符串或其他单个字符串叶子可能混合命令语法和玩家可见文本。不要因为混有命令语法就一概排除；如果玩家可见文本明确存在，且命令名、参数名、资源名、分隔符等不可翻译协议片段边界稳定，可以先把该字符串叶子列入规则，并在完成报告中提醒主代理后续必须用占位符规则保护协议壳。若边界不稳定、看不出哪部分会显示给玩家，或协议壳无法安全保护，则排除并报告风险。
- 必须交叉验证每个选中规则：至少用同一编码下的参数形态、重复出现次数、相邻参数、对象键名或值分布中的两个证据互相核对；证据不足时排除并报告风险。
- 不确定字段先看同一指令内相邻参数、对象键名和值的语义；仍不确定时排除，并在完成报告写明风险。

## 输出

- 唯一可写文件：`<工作区>/event-command-rules.json`。
- 工作报告：`<工作区>/agent-reports/external_rules/event_command_rules_discovery.json`。
- 脚本和统计：`<工作区>/agent-scratch/external_rules/event_command_rules_discovery/`。
- 顶层必须是对象，格式为 `{指令编码字符串: [{match, paths}]}`。
- `match` 必须是对象，键为参数索引字符串，值为对应参数的精确字符串值。
- `paths` 必须是 JSONPath 字符串数组，路径从 `$['parameters']` 开始，并命中字符串叶子。
- JSONPath 必须使用括号路径语法；禁止使用 `$.xxx` 点号路径。
- 合法空结果是 `{}`，表示已确认没有可导入事件指令参数规则。

中性格式示例：

```json
{
  "<指令编码>": [
    {
      "match": {
        "0": "<用于区分形态的参数值>"
      },
      "paths": [
        "$['parameters'][3]['<玩家可见文本字段>']"
      ]
    }
  ],
  "<另一个指令编码>": [
    {
      "match": {},
      "paths": [
        "$['parameters'][<参数索引>]"
      ]
    }
  ]
}
```

## 校验

写完后建议主代理运行：

```powershell
uv run python main.py validate-event-command-rules --game <游戏标题> --input <工作区>/event-command-rules.json
```

如果校验失败，只修 `<工作区>/event-command-rules.json` 后重跑同一校验命令；不要绕过 CLI 导入。

## 完成报告

完成时报告：

- 改动文件。
- 读取文件、写入脚本、统计产物和运行过的只读/validate 命令。
- 命中的指令编码、规则数量和路径数量。
- 程序候选之外的主动发现。
- 是否为空结果；为空时说明检查范围和空结果理由。
- 交叉验证摘要：选中项依据、重点排除项依据、空结果依据、仍需主代理确认的指令形态。
- 主要排除类别。
- 未解决风险；如果纳入了混合协议文本，必须列出需要主代理后续用占位符规则保护的协议片段或边界。
- 建议主代理运行的校验命令。
